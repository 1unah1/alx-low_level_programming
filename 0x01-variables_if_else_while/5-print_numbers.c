#include <stdio.h>

/**
 * main - Entry point
 * Description: 'Function to print from 0 - 9 amd a mewline'
 * Return: Always 0 (success)
 */

int main(void)
{
	int strt = 48;

	for (strt = 48; strt <= 57; strt++)
	{
		printf("%c", strt);
	}

putchar('\n');

return (0);
}
