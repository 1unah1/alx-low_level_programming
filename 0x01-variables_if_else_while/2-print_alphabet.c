#include <stdio.h>

/**
 * main - Entry point
 * Description: 'This function printee the alphabet
 * lower case and then prints a new line'
 * Return: Always 0 (Success)
 */

int main(void)
{
	int alph = 'a';

	while (alph <= 'z')
	{
		putchar(alph)
			alph++;
	}

putchar('\n');
return (0);
}
