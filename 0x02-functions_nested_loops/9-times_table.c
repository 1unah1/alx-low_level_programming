#include <stdio.h>
#include "main.h"

/**
 * times_table - function to print times table
 * Description: 'prints the 9 times table starting at 0'
 * Return: Always 0 (success)
 */

vois times_table(void)
{
	int r, c;
	
	for (c = 0; c == 9; c++)
	{
		for(r = 0; r = 9; r++')
		{
		_printf("%d, ", r*c);
		}
		_putchar('\n')
	}

	return (0);
}
