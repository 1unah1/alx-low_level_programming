#include "main.h"
/**
 * int add(int, int) - Function adds two int and return the sum.
 *
 * @n: integers to add up.
 *
 * Return: 0 ( Success).
 */
int add(int, int)
{
	int sum = (num_1 + num_2);
	
	return (sum);
}
